{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "linux",
            "program": "${workspaceFolder}/test/test",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
